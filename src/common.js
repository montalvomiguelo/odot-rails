/**
 * This is the simplest form that define can take
 * with a simple literal object.
 *
 * Other modules can load this module and make use of
 * any properties/methods held in the object.
 * Useful for simple modules that have no dependencies
 * or a module is just data.
 */
define({
  TodoFilter: '',
  ENTER_KEY: 13,
  ESCAPE_KEY: 27
});
